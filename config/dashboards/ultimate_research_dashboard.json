{
  "dashboard": {
    "id": null,
    "title": "NCS Self-Healing: Ultimate Research Dashboard",
    "tags": ["ncs", "research", "self-healing", "control-systems"],
    "timezone": "browser",
    "panels": [
      {
        "id": 1,
        "title": "🎯 Experiment Phase Control",
        "type": "stat",
        "gridPos": {"h": 3, "w": 24, "x": 0, "y": 0},
        "targets": [{
          "query": "SELECT last(\"experiment_phase\") FROM \"ncs_experiments\" WHERE time > now() - 5m",
          "rawQuery": true
        }],
        "fieldConfig": {
          "defaults": {
            "color": {"mode": "value"},
            "mappings": [
              {"options": {"BASELINE": {"color": "green", "text": "🟢 BASELINE PHASE"}}, "type": "value"},
              {"options": {"ATTACK": {"color": "red", "text": "🔴 ATTACK PHASE"}}, "type": "value"},
              {"options": {"RECOVERY": {"color": "yellow", "text": "🟡 RECOVERY PHASE"}}, "type": "value"}
            ]
          }
        }
      },
      {
        "id": 2,
        "title": "📊 Stability Margin Analysis",
        "type": "timeseries",
        "gridPos": {"h": 8, "w": 12, "x": 0, "y": 3},
        "targets": [
          {
            "query": "SELECT mean(\"stability_margin\") FROM \"ncs_experiments\" WHERE time > now() - 30m GROUP BY time(10s), \"experiment_phase\" fill(null)",
            "alias": "Stability by Phase",
            "rawQuery": true
          }
        ],
        "fieldConfig": {
          "defaults": {
            "color": {"mode": "palette-classic"},
            "custom": {
              "drawStyle": "line",
              "lineInterpolation": "smooth",
              "pointSize": 5,
              "fillOpacity": 10
            },
            "unit": "percentunit",
            "min": 0,
            "max": 1
          }
        }
      },
      {
        "id": 3,
        "title": "💰 Control Cost Efficiency",
        "type": "timeseries",
        "gridPos": {"h": 8, "w": 12, "x": 12, "y": 3},
        "targets": [
          {
            "query": "SELECT mean(\"control_cost\") FROM \"ncs_experiments\" WHERE time > now() - 30m GROUP BY time(10s), \"experiment_phase\" fill(null)",
            "alias": "Control Cost",
            "rawQuery": true
          }
        ],
        "fieldConfig": {
          "defaults": {
            "color": {"mode": "palette-classic"},
            "unit": "short",
            "min": 0,
            "custom": {
              "drawStyle": "line",
              "lineInterpolation": "smooth"
            }
          }
        }
      },
      {
        "id": 4,
        "title": "⚡ Mean Time to Recovery (MTTR)",
        "type": "timeseries",
        "gridPos": {"h": 8, "w": 12, "x": 0, "y": 11},
        "targets": [
          {
            "query": "SELECT mean(\"mttr_seconds\") FROM \"ncs_experiments\" WHERE time > now() - 30m GROUP BY time(10s), \"experiment_phase\" fill(null)",
            "alias": "MTTR",
            "rawQuery": true
          }
        ],
        "fieldConfig": {
          "defaults": {
            "color": {"mode": "palette-classic"},
            "unit": "s",
            "min": 0,
            "custom": {
              "drawStyle": "line",
              "lineInterpolation": "smooth"
            }
          }
        }
      },
      {
        "id": 5,
        "title": "🔄 Recovery Count & Success Rate",
        "type": "timeseries",
        "gridPos": {"h": 8, "w": 12, "x": 12, "y": 11},
        "targets": [
          {
            "query": "SELECT sum(\"recovery_count\") FROM \"ncs_experiments\" WHERE time > now() - 30m GROUP BY time(30s) fill(0)",
            "alias": "Recovery Events",
            "rawQuery": true
          },
          {
            "query": "SELECT mean(\"recovery_success_rate\") * 100 FROM \"ncs_experiments\" WHERE time > now() - 30m GROUP BY time(30s) fill(null)",
            "alias": "Success Rate %",
            "rawQuery": true
          }
        ],
        "fieldConfig": {
          "defaults": {
            "color": {"mode": "palette-classic"},
            "custom": {
              "drawStyle": "line",
              "lineInterpolation": "smooth"
            }
          },
          "overrides": [
            {
              "matcher": {"id": "byName", "options": "Recovery Events"},
              "properties": [{"id": "unit", "value": "short"}]
            },
            {
              "matcher": {"id": "byName", "options": "Success Rate %"},
              "properties": [{"id": "unit", "value": "percent"}]
            }
          ]
        }
      },
      {
        "id": 6,
        "title": "🌐 Network Quality Metrics",
        "type": "timeseries",
        "gridPos": {"h": 8, "w": 8, "x": 0, "y": 19},
        "targets": [
          {
            "query": "SELECT mean(\"network_latency_ms\") FROM \"ncs_experiments\" WHERE time > now() - 30m GROUP BY time(10s) fill(null)",
            "alias": "Latency (ms)",
            "rawQuery": true
          },
          {
            "query": "SELECT mean(\"packet_loss_rate\") * 100 FROM \"ncs_experiments\" WHERE time > now() - 30m GROUP BY time(10s) fill(null)",
            "alias": "Packet Loss %",
            "rawQuery": true
          }
        ],
        "fieldConfig": {
          "defaults": {
            "color": {"mode": "palette-classic"},
            "custom": {
              "drawStyle": "line",
              "lineInterpolation": "smooth"
            }
          }
        }
      },
      {
        "id": 7,
        "title": "📡 Network Jitter & Throughput",
        "type": "timeseries",
        "gridPos": {"h": 8, "w": 8, "x": 8, "y": 19},
        "targets": [
          {
            "query": "SELECT mean(\"network_jitter_ms\") FROM \"ncs_experiments\" WHERE time > now() - 30m GROUP BY time(10s) fill(null)",
            "alias": "Jitter (ms)",
            "rawQuery": true
          },
          {
            "query": "SELECT mean(\"network_throughput_mbps\") FROM \"ncs_experiments\" WHERE time > now() - 30m GROUP BY time(10s) fill(null)",
            "alias": "Throughput (Mbps)",
            "rawQuery": true
          }
        ],
        "fieldConfig": {
          "defaults": {
            "color": {"mode": "palette-classic"},
            "custom": {
              "drawStyle": "line",
              "lineInterpolation": "smooth"
            }
          }
        }
      },
      {
        "id": 8,
        "title": "🎯 System Performance Score",
        "type": "gauge",
        "gridPos": {"h": 8, "w": 8, "x": 16, "y": 19},
        "targets": [{
          "query": "SELECT mean(\"system_resilience_score\") FROM \"ncs_experiments\" WHERE time > now() - 5m",
          "rawQuery": true
        }],
        "fieldConfig": {
          "defaults": {
            "color": {
              "mode": "thresholds"
            },
            "mappings": [],
            "thresholds": {
              "steps": [
                {"color": "red", "value": 0},
                {"color": "yellow", "value": 0.5},
                {"color": "green", "value": 0.7}
              ]
            },
            "unit": "percentunit",
            "min": 0,
            "max": 1
          }
        }
      },
      {
        "id": 9,
        "title": "📈 Statistical Analysis - Phase Comparison",
        "type": "barchart",
        "gridPos": {"h": 8, "w": 12, "x": 0, "y": 27},
        "targets": [
          {
            "query": "SELECT mean(\"stability_margin\") FROM \"ncs_experiments\" WHERE time > now() - 30m GROUP BY \"experiment_phase\"",
            "alias": "Avg Stability",
            "rawQuery": true
          },
          {
            "query": "SELECT mean(\"control_cost\") FROM \"ncs_experiments\" WHERE time > now() - 30m GROUP BY \"experiment_phase\"",
            "alias": "Avg Control Cost",
            "rawQuery": true
          }
        ],
        "fieldConfig": {
          "defaults": {
            "color": {"mode": "palette-classic"},
            "custom": {
              "hideFrom": {
                "legend": false,
                "tooltip": false,
                "vis": false
              }
            }
          }
        }
      },
      {
        "id": 10,
        "title": "🚀 Real-time Key Performance Indicators",
        "type": "stat",
        "gridPos": {"h": 8, "w": 12, "x": 12, "y": 27},
        "targets": [
          {
            "query": "SELECT last(\"stability_margin\") FROM \"ncs_experiments\" WHERE time > now() - 1m",
            "alias": "Current Stability",
            "rawQuery": true
          },
          {
            "query": "SELECT last(\"mttr_seconds\") FROM \"ncs_experiments\" WHERE time > now() - 1m",
            "alias": "Latest MTTR",
            "rawQuery": true
          },
          {
            "query": "SELECT last(\"control_efficiency\") FROM \"ncs_experiments\" WHERE time > now() - 1m",
            "alias": "Control Efficiency",
            "rawQuery": true
          },
          {
            "query": "SELECT last(\"network_quality_score\") FROM \"ncs_experiments\" WHERE time > now() - 1m",
            "alias": "Network Quality",
            "rawQuery": true
          }
        ],
        "fieldConfig": {
          "defaults": {
            "color": {"mode": "value"},
            "mappings": [],
            "thresholds": {
              "steps": [
                {"color": "red", "value": 0},
                {"color": "yellow", "value": 0.5},
                {"color": "green", "value": 0.8}
              ]
            }
          }
        }
      },
      {
        "id": 11,
        "title": "🔬 Research Experiment Timeline",
        "type": "timeseries",
        "gridPos": {"h": 6, "w": 24, "x": 0, "y": 35},
        "targets": [
          {
            "query": "SELECT \"experiment_phase\" FROM \"ncs_experiments\" WHERE time > now() - 30m",
            "rawQuery": true
          }
        ],
        "fieldConfig": {
          "defaults": {
            "color": {"mode": "palette-classic"},
            "custom": {
              "drawStyle": "line",
              "lineInterpolation": "stepAfter",
              "pointSize": 8,
              "fillOpacity": 20
            },
            "mappings": [
              {"options": {"BASELINE": {"color": "green", "text": "BASELINE"}}, "type": "value"},
              {"options": {"ATTACK": {"color": "red", "text": "ATTACK"}}, "type": "value"},
              {"options": {"RECOVERY": {"color": "yellow", "text": "RECOVERY"}}, "type": "value"}
            ]
          }
        }
      }
    ],
    "time": {
      "from": "now-30m",
      "to": "now"
    },
    "refresh": "5s",
    "schemaVersion": 37,
    "version": 0,
    "weekStart": ""
  }
}
